(function( $ ) {
	'use strict';

	$( document ).ready(function(){

		// add custom class to parent <a> selector
		// if( ! $('a.addonify-qvm-overlay-button').length ){

		// 	let $overlay_btn_sel = $('button.addonify-overlay-image');
		// 	$overlay_btn_sel.each(function(){
		// 		$(this).parents('a').addClass('addonify-qvm-overlay-button');
		// 	})
		// }

	})

})( jQuery );
